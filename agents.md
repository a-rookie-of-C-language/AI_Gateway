每个类一个文件，文件名就是类名
每个结构体/trait一个文件，文件名就是结构体名/trait名
业务代码不允许直接操作数据库，只能通过DAO层操作数据库
改代码前先问我是否需要改,如果有多个方案,请给出优缺点我确认后才改代码
python环境使用venv,优先使用项目的.venv环境
每次改代码后,必须先执行mvn -q -DskipTests spotless:check等命令检查代码格式是否符合规范
```bash
d:\python\python.exe" -m venv .venv
```
then activate the venv
```bash
agent\.venv\Scripts\activate
```
全部用中文回答,包括思考过程
必须遵循以下工作步骤：
 1.分析实现需求时需要确认的细节并向用户逐一确认
 2.根据扩充后的需求以及细节，编写改动方案（简要说明原先有什么问题，新方案为什么采用并注明改动的文件、改动的位置、改动的内容），必须由用户确认后才能进行下一步
 3.落地改动方案 严禁跳过下一步的验收 将第四步加入todolist中
 4.验收改动方案
 5.如果改动方案符合预期，将第四步从todolist中移除
 6.标记所有todo list全部完成后才能结束
必须遵守以下工作规范：
 1.禁止做计划以外的修改，但可以提出建议，用户确认后可以实施
 2.修复Bug时，必须由用户确认后才能实施
 3.你不能直接提交commit消息,只能给我消息
 4.每次完成一个功能时自动给我commit消息,使用中文描述,采用git commit规范例如
 feat: 新增功能
 fix: 修复功能
 refactor: 重构功能
 docs: 文档变更
 test: 测试变更
 style: 样式变更
 perf: 性能变更
 chore: 架构变更
 merge: 合并分支

